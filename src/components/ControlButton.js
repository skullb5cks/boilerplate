import React from 'react'import Button from '../containers/ButtonClick'const ControlButton = () => (    <p>        Control Button:        {' '}        <Button filter="REMOVE_ALL">            Remove All        </Button>        {' '}        <Button filter="REMOVE_RECENT">            Remove Recent        </Button>        {' '}        <Button filter="REMOVE_COMPLETED">            Remove completed        </Button>    </p>);export default ControlButton;